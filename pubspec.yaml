name: cashvault_local
description: CashVault Local - offline cash counting for desktop.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  drift: ^2.32.0
  sqlite3: ^3.0.0
  sqlcipher_flutter_libs: ^0.7.0+eol
  path_provider: ^2.1.5
  path: ^1.9.1
  file_picker: ^10.3.3
  csv: ^6.0.0
  intl: ^0.20.2
  cryptography: ^2.7.0
  provider: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.32.0
  build_runner: ^2.10.1

flutter:
  uses-material-design: true
